Goal:
a window of buttons, lain out scrollable or in a grid.
[ Address ]
hit the gear button at the end to edit, delete, or create entries
Each entry is stored in a json file.
If I wanted to go real crazy, I could encrypt the text at each option.
each button entry is stored in a json
you click a button to copy the text you stored
